import axiosInstance from "../api/axios";

const Unauth = () => {
    // async function handleRole(){
    //     const res = await axiosInstance(post)
    // }
    return ( 
    <div>
        You are not have authen in this page
    </div> );
}
 
export default Unauth;